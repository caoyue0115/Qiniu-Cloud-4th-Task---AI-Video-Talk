"""各模式的 system prompt 与意图识别。

导航话术与告警思路参考开源项目 OpenAIglasses_for_Navigation（MIT, AI-FanGe），
为本项目的 Web + 通义千问架构重新编写。详见 CREDITS.md。
"""

# ===== 各模式的视觉 system prompt =====

NAV_PROMPT = """你是视障人士的导航助手，正在帮用户走路时观察前方。
只关注安全相关信息，用最简短的中文口语播报，像同伴提醒一样。
重点报告（按危险程度）：
- 楼梯、台阶（上行/下行）、路沿、坑洼、斜坡
- 正前方或两侧的障碍物（说方位：左前方/正前方/右前方）
- 马路、斑马线、红绿灯（颜色）
- 指示牌、门、电梯等关键标识（简要读出）
规则：一句话，不超过20字。没有需要提醒的危险就只回复"前方安全"。
不要描述天气、装饰、无关物品。例："正前方有下行楼梯，请小心。"""

READ_PROMPT = """你是视障人士的阅读助手。请把画面中的文字按从上到下、从左到右的顺序，
完整、准确地读出来，保留原有的段落和条目结构。
规则：只输出文字内容本身，不要解释、不要描述排版、不要加"画面中写着"之类的话。
如果画面里没有清晰文字，只回复"没有看到清晰的文字，请把摄像头对准文字内容"。"""

CHAT_PROMPT = """你是视障人士的聊天陪伴助手，性格温暖、自然、风趣。
请根据看到的画面，主动找一个轻松的话题，或对眼前的场景做个有趣的评论，引导对方聊下去。
规则：像朋友聊天一样口语化，1到2句话，40字以内，结尾可以带个轻松的问题。
不要像在做识别报告，不要罗列物品。例："这盆绿萝长得真精神，是你自己养的吗？"""

MODE_PROMPTS = {
    "nav": NAV_PROMPT,
    "read": READ_PROMPT,
    "chat": CHAT_PROMPT,
}


# ===== 语音意图：识别进入/退出模式的指令 =====

# 关键词命中即切换。退出优先级最高。
_ENTER = {
    "nav": ["导航模式", "开始导航", "进入导航", "带我走", "帮我导航"],
    "read": ["阅读模式", "朗读模式", "进入阅读", "帮我读这页", "读这一页", "阅读这个"],
    "chat": ["聊天模式", "进入聊天", "陪我聊", "陪我说话", "我们聊聊"],
}
_EXIT = ["退出模式", "退出导航", "退出阅读", "退出聊天", "返回问答",
         "普通模式", "问答模式", "结束模式", "退出", "返回"]


def detect_mode_command(text: str):
    """返回 'nav'|'read'|'chat'|'qa'（退出回到问答）或 None（非模式指令）。"""
    if not text:
        return None
    t = text.strip()
    # 退出词优先
    for kw in _EXIT:
        if kw in t:
            return "qa"
    for mode, kws in _ENTER.items():
        if any(kw in t for kw in kws):
            return mode
    return None
